// http://www.geeksforgeeks.org/custom-tree-problem

#include <iostream>
#include <algorithm>
#include <vector>
#include "bt.h"

node *createTree(string *a, int n){
	node *tnode[26];
	for(int i=0; i<n; i++){
		
		if(tnode[])
	}
	
}

void printForest(string *a){
	node *root = createTree(a);
}

int main(){
	string arr[] = {"a b", "b c", "b d", "a e"};
	printForest(arr, 4);
	return 0;
}

